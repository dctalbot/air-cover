---
name: add-rest-endpoint
description: Use this to implement a REST endpoint to the application.
---

1. Specify the new endpoint in `api/openapi.yaml`.
2. Run `make generate` to create the boilerplate server code.
3. Run `make check` to verify that the code changes are correct.
